import React from 'react';
import { ActivityHome } from '../components/ActivityList';

const sampleActivity = {
  title: 'Title',
  text: 'Text',
  image: 'image'
};

const Home = () => {};

export default Home;
