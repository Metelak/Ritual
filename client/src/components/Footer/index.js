import React from 'react';

const Footer = () => {
    return (
        <footer>
            <div>
            From the CRMMMM developers &copy;{new Date().getFullYear()}
            </div>
        </footer>
    );
};

export default Footer;